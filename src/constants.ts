export const VERSION = '1.0.1';
export const OUTPUT_DIR = 'output';
export const MANIFEST_FILE = `${OUTPUT_DIR}/avatarManifest-${VERSION}.json`;
export const IMAGE_FILE_NAMES = `${OUTPUT_DIR}/imageFileNames-${VERSION}.json`;
export const IMAGES_META_FILE = `${OUTPUT_DIR}/imagesMeta-${VERSION}.json`;